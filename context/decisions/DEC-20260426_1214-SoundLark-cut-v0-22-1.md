---
apiVersion: processkit.projectious.work/v2
kind: DecisionRecord
metadata:
  id: DEC-20260426_1214-SoundLark-cut-v0-22-1
  created: '2026-04-26T12:14:18+00:00'
spec:
  title: Cut v0.22.1 today (RoyalFern alone), target v0.23.0 as focused batch
  state: accepted
  decision: |
    Tag and ship v0.22.1 today carrying the RoyalFern Model schema additions (jurisdiction, data_privacy, knowledge_cutoff, vendor_model_id, latency_p50_ms — already on main at 4a0e1d6 + the 34-model backfill at 8e914d7) and the model-recommender SKILL.md Governance-dimension docs that go with them. Nothing else in the patch.

    Then prepare v0.23.0 as a focused batch comprising: (a) model-recommender query_models() filter implementation (newly-filed WI — gives RoyalFern fields runtime teeth), (b) VastLark (skill-gate PreToolUse matcher auto-renew gap), (c) RapidDaisy (log_event MCP actor required-field validation), (d) CalmArch (workitem-management YAML scalar serialization). Hold the v0.23.0 tag until owner reviews backlog for additional inclusions.
  context: 'Morning handover (LOG-20260426_1154-JollyMoss) flagged a release-shape
    decision pending: RoyalFern (5 additive Model schema fields backfilled across
    all 34 models, plus SKILL.md docs) is shippable but untagged on main, and the
    owner had to choose between a one-feature point release vs. a coherent batch.
    Afternoon investigation (this session) confirmed Phase B of WildGrove also landed
    in aibox v0.20.0 (preauth merge), so that work item closed independently and does
    not affect the release shape.'
  rationale: 'RoyalFern is on main and shippable as-is. Holding it back to bundle
    with v0.23.0 produces zero value — consumers waiting on the schema can adopt sooner.
    The natural pair-up (filter implementation that lets callers actually filter by
    RoyalFern fields) is not yet filed and has unbounded surface area; pinning it
    to a v0.23.0 batch lets us scope it properly. Two tags in quick succession is
    minor ceremony cost vs. real consumer benefit. Semver also lines up cleanly: additive
    optional schema fields → patch bump.'
  alternatives:
  - option: Single v0.23.0 release bundling RoyalFern + filter-impl + VastLark + RapidDaisy
      + CalmArch
    rejected_because: Filter implementation isn't filed yet; bundling it would block
      the schema-only ship indefinitely while scope is decided. One announcement is
      nicer-to-have but not worth blocking shipped work.
  - option: Hold RoyalFern indefinitely on main, cut nothing
    rejected_because: Defeats the purpose of having shipped work on main. Sitting
      on additive schema fields helps no consumer.
  consequences: Two release tags within roughly a week instead of one. v0.22.1 release
    notes scope is small (one feature). v0.23.0 milestone now has a defined floor
    (4 confirmed items + filter-impl) that owner can extend before tagging. The cross-project
    FierceWren and CleverRiver bugs are upstreamed to aibox via GitHub issues — no
    processkit release work tied to them.
  related_workitems:
  - BACK-20260425_2256-RoyalFern-model-schema-add-jurisdiction
  - BACK-20260425_1235-VastLark-skill-gate-expand-pretooluse
  - BACK-20260425_1755-RapidDaisy-processkit-event-log-log
  - BACK-20260425_1755-CalmArch-workitem-management-mcp-serializes
  decided_at: '2026-04-26T12:14:18+00:00'
---
