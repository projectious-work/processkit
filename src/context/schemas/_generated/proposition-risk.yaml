# Generated by processkit schema generation. Do not edit directly.
apiVersion: processkit.projectious.work/v2
kind: Schema
metadata:
  id: SCHEMA-proposition-risk
  target_kind: Proposition
  version: 1.0.0-alpha.1
  created: 2026-07-22 00:00:00+00:00
spec:
  interfaces:
  - Entity
  - Versioned
  - Proposition
  validation_mode: tolerant
  generation:
    source: context/schemas/src
    format: processkit-schema-source-v1
  description: 'A Proposition about uncertainty, likelihood, impact, response, mitigation,
    contingency, and ownership.

    '
  id_prefix: PROP
  state_machine: null
  default_directory: context/propositions
  known_kinds:
  - claim
  - risk
  spec_schema:
    $schema: https://json-schema.org/draft/2020-12/schema
    type: object
    required:
    - kind
    - statement
    - likelihood
    - impact
    additionalProperties: true
    properties:
      kind:
        type: string
        enum:
        - claim
        - risk
        description: Proposition discriminator.
        const: risk
      statement:
        type: string
        minLength: 1
        description: The claim expressed by this Proposition.
      status:
        type: string
        enum:
        - open
        - supported
        - refuted
        - withdrawn
        - superseded
        description: Current epistemic status.
      confidence:
        type: number
        minimum: 0
        maximum: 1
        description: Confidence from zero to one.
      owner:
        type:
        - string
        - 'null'
        pattern: ^(ACTOR|TEAMMEMBER)-[a-zA-Z0-9_-]+$
        description: Accountable Actor or TeamMember.
      evidence:
        type: array
        items:
          type: string
        description: Entity IDs or external evidence references.
      scope:
        type:
        - string
        - 'null'
        description: Scope in which the claim applies.
      source:
        type:
        - string
        - 'null'
        description: Origin of the claim.
      valid_from:
        type: string
        format: date-time
      valid_until:
        type: string
        format: date-time
      supersedes:
        type:
        - string
        - 'null'
        pattern: ^PROP-[a-zA-Z0-9_-]+$
      likelihood:
        type: string
        enum:
        - rare
        - unlikely
        - possible
        - likely
        - almost-certain
      probability:
        type: number
        minimum: 0
        maximum: 1
      impact:
        type: string
        enum:
        - negligible
        - minor
        - moderate
        - major
        - critical
      risk_status:
        type: string
        enum:
        - identified
        - analyzing
        - accepted
        - mitigating
        - realized
        - closed
      response:
        type: string
        enum:
        - avoid
        - mitigate
        - transfer
        - accept
        - escalate
      mitigation:
        type: string
      contingency:
        type: string
      affected_entities:
        type: array
        items:
          type: string
      review_at:
        type: string
        format: date-time
      realized_at:
        type: string
        format: date-time
  parent: Proposition
  discriminator:
    field: kind
    value: risk
