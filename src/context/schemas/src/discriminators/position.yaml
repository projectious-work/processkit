extends: ../primitives/binding.yaml
apiVersion: processkit.projectious.work/v2
kind: Schema
metadata: {id: SCHEMA-binding-position, target_kind: Binding, version: 1.0.0-beta.1}
spec:
  parent: Binding
  discriminator: {field: type, value: position}
  description: A role slot whose subject may remain vacant.
  spec_schema:
    required: {__merge: replace, value: [type, target]}
    properties:
      __merge: name-merge
      value:
        type: {const: position}
        subject: {type: [string, "null"]}
        seniority: {type: [string, "null"]}
