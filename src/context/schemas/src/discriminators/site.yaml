extends: ../primitives/location.yaml
apiVersion: processkit.projectious.work/v2
kind: Schema
metadata: {id: SCHEMA-location-site, target_kind: Location, version: 1.0.0-alpha.3}
spec:
  parent: Location
  discriminator: {field: kind, value: site}
  description: A physical facility or operating site.
  spec_schema:
    properties:
      __merge: name-merge
      value:
        kind: {const: site}
        value: {type: string, minLength: 1}
