extends: ../primitives/binding.yaml
apiVersion: processkit.projectious.work/v2
kind: Schema
metadata: {id: SCHEMA-binding-provenancelink, target_kind: Binding, version: 1.0.0-alpha.3}
spec:
  parent: Binding
  discriminator: {field: type, value: provenance-link}
  description: A relation from derived content to its source or evidence.
  spec_schema:
    properties:
      __merge: name-merge
      value: {type: {const: provenance-link}}
