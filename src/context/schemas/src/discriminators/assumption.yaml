extends: ../primitives/proposition.yaml
apiVersion: processkit.projectious.work/v2
kind: Schema
metadata: {id: SCHEMA-proposition-assumption, target_kind: Proposition, version: 1.0.0-beta.1}
spec:
  parent: Proposition
  discriminator: {field: kind, value: assumption}
  description: A premise accepted temporarily to enable action.
  spec_schema:
    properties:
      __merge: name-merge
      value:
        kind: {const: assumption}
        validation_due: {type: [string, "null"], format: date-time}
        validation_method: {type: [string, "null"]}
