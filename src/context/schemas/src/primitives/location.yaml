extends: [../fragments/entity.yaml, ../fragments/versioned.yaml]
apiVersion: processkit.projectious.work/v2
kind: Schema
metadata: {id: SCHEMA-location, target_kind: Location, version: 1.0.0-beta.1}
spec:
  description: A physical, geographic, logical, coordinate, or timezone anchor.
  id_prefix: LOC
  state_machine: null
  default_directory: context/locations
  known_kinds: [geographic-region, site, coordinate, logical-region, timezone]
  spec_schema:
    type: object
    required: [name, kind]
    additionalProperties: true
    properties:
      name: {type: string, minLength: 1}
      kind:
        type: string
        enum: [geographic-region, site, coordinate, logical-region, timezone]
      value: {}
