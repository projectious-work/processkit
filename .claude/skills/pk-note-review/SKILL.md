---
name: pk-note-review
description: Use the note-management skill to review all fleeting notes and decide what to promote or discard.
---

Use the note-management skill to review all fleeting notes and decide what to promote or discard.


---

This command is a processkit skill shim. Load and follow the matching skill for `pk-note-review` from `context/skills/` instead of executing underlying helper scripts directly. Do not run `context/skills/**/scripts/*.py`, `doctor.py`, or `uv run .../scripts/...` unless the skill instructions explicitly require that implementation detail for the current step.
