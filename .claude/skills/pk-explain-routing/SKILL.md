---
name: pk-explain-routing
description: Use the model-recommender skill to explain the model-routing trace for $ARGUMENTS.
---

Use the model-recommender skill to explain the model-routing trace for $ARGUMENTS.


---

This command is a processkit skill shim. Load and follow the matching skill for `pk-explain-routing` from `context/skills/` instead of executing underlying helper scripts directly. Do not run `context/skills/**/scripts/*.py`, `doctor.py`, or `uv run .../scripts/...` unless the skill instructions explicitly require that implementation detail for the current step.
