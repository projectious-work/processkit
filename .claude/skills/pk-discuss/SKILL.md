---
name: pk-discuss
description: Use the discussion-management skill to start a structured discussion
---

Use the discussion-management skill to start a structured discussion
on $ARGUMENTS. Research the topic, challenge assumptions, present
alternatives, and work toward a conclusion that may produce a Note,
DecisionRecord, or WorkItem.


---

This command is a processkit skill shim. Load and follow the matching skill for `pk-discuss` from `context/skills/` instead of executing underlying helper scripts directly. Do not run `context/skills/**/scripts/*.py`, `doctor.py`, or `uv run .../scripts/...` unless the skill instructions explicitly require that implementation detail for the current step.
