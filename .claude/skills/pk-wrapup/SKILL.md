---
name: pk-wrapup
description: Use the session-handover skill to generate an end-of-session handover
---

Use the session-handover skill to generate an end-of-session handover
document.


---

This command is a processkit skill shim. Load and follow the matching skill for `pk-wrapup` from `context/skills/` instead of executing underlying helper scripts directly. Do not run `context/skills/**/scripts/*.py`, `doctor.py`, or `uv run .../scripts/...` unless the skill instructions explicitly require that implementation detail for the current step.
