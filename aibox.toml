# =============================================================================
# aibox.toml — single source of truth for your aibox project.
# All .devcontainer/ files are generated from this. Edit here, run `aibox apply`.
# Reference: https://projectious-work.github.io/aibox/docs/reference/configuration
# =============================================================================

# Object identity. These root keys mirror Kubernetes-style resource files:
# apiVersion selects the aibox config API; kind is currently always Workspace.
apiVersion = "aibox.projectious.work/v1"
kind       = "Workspace"

[aibox]
project_name = "processkit"         # Human-readable project name; defaults to container.name
profile      = "human-dev"       # Usage profile. Options: human-dev, headless-runner

# =============================================================================
# [container] — runtime and build configuration
# =============================================================================
[container]
name     = "processkit"         # Container name used by docker/podman
hostname = "processkit"         # Hostname visible inside the container
user     = "aibox"              # User inside the container (controls mount paths)

[container.image]
release_version = "latest"       # Target aibox image/CLI version. Use "latest" to resolve newest on apply.
base            = "debian"          # Published base image flavor. Options: debian

[container.paths]
devcontainer_json     = ".devcontainer/devcontainer.json"
docker_compose        = ".devcontainer/docker-compose.yml"
docker_compose_override = ".devcontainer/docker-compose.override.yml"
dockerfile            = ".devcontainer/Dockerfile"
dockerfile_local      = ".devcontainer/Dockerfile.local"
local_env             = ".aibox-local.env"  # Generated from .aibox-local.toml for docker compose env_file

# --- Lifecycle ---
[container.lifecycle]
# post_create_command = "npm install"  # Shell command run once after container first starts
# keepalive           = true           # Send periodic keepalive (prevents NAT idle dropout in OrbStack/VMs)

# --- Resource pressure warnings (`aibox doctor`) ---
# [container.resource_thresholds]
# memory_mib_warn = 4096       # Optional cgroup memory warning threshold in MiB
# process_count_warn = 400     # Set to 0 to disable this warning
# processkit_mcp_python_warn = 50  # Expected to drop after processkit gateway adoption
# oom_kill_warn = 0            # Warn when cgroup OOM kill count is greater than this

# =============================================================================
# [skills] — processkit skill catalog
# =============================================================================
# aibox now installs the full product skill set by default. Use enabled[] for
# explicit additions and disabled[] for explicit removals. Core skills are
# always installed; disabling one only triggers a doctor warning.
[skills]
enabled = [
    # "actor-profile", # explicitly enable; processkit; Create and maintain Actor entities — humans and AI agents that participate in the project. Use when adding a …
    # "agent-card", # explicitly enable; processkit; Project processkit agent-card Artifacts into canonical public agent-card JSON files. Use when publishing an a…
    # "agent-management", # explicitly enable; processkit; Orchestrate multi-agent workflows with task decomposition, role assignment, handoff protocols, and per-sessio…
    # "aggregate-mcp", # explicitly enable; processkit; Expose the processkit MCP tool surface through a single stdio server. Use for harnesses that eagerly start ev…
    # "ai-fundamentals", # explicitly enable; data-ai; Explain and apply core ML/AI concepts — model types, training pipelines, evaluation metrics, and neural archi…
    # "alerting-oncall", # explicitly enable; devops; Alert design and on-call practices — severity, runbooks, SLO burn-rate alerting, escalation. Use when designi…
    # "api-design", # explicitly enable; engineering; REST API design — resource naming, HTTP methods, status codes, pagination, versioning, OpenAPI. Use when desi…
    # "artifact-management", # explicitly enable; processkit; Create, retrieve, query, and update Artifacts — tangible deliverables produced by the project. Use when stori…
    # "auth-patterns", # explicitly enable; engineering; Authentication and authorization patterns — OAuth2, JWT, sessions, RBAC/ABAC. Use when implementing login flo…
    # "binding-management", # explicitly enable; processkit; Manage Binding entities — scoped, temporal, many-to-many relationships between v2 entity surfaces. Use when a…
    # "board-of-advisors", # explicitly enable; product; Simulates a panel of expert advisors who each evaluate a problem from their distinct perspective — surfacing …
    # "caching-strategies", # explicitly enable; engineering; Caching patterns — cache-aside, write-through, TTL strategies, invalidation, HTTP caching. Use when adding ca…
    # "category-management", # explicitly enable; processkit; Manage Category entities — taxonomies and classification schemes that group other entities by type, area, tie…
    # "changelog", # explicitly enable; engineering; Generates and maintains CHANGELOG.md files following Keep a Changelog conventions — from git history, commit …
    # "ci-cd-setup", # explicitly enable; devops; CI/CD pipeline setup — GitHub Actions, testing, linting, deployment. Use when setting up CI from scratch, add…
    # "code-generation", # explicitly enable; engineering; Code generation patterns — template engines, AST manipulation, scaffolding, build-step generation. Use when b…
    # "code-review", # explicitly enable; engineering; Structured code review with a checklist covering correctness, clarity, tests, security, performance, and styl…
    # "concurrency-patterns", # explicitly enable; engineering; Concurrency and parallelism patterns — async/await, threads, actors, channels, deadlock prevention. Use when …
    # "constraint-management", # explicitly enable; processkit; Manage Constraint entities — rules and limits the project must respect (budget, latency SLO, team size, compl…
    # "container-orchestration", # explicitly enable; devops; Docker Compose patterns for multi-service apps — health checks, networking, volumes, dependencies. Use when w…
    # "context-archiving", # explicitly enable; processkit; Plan and execute hot-to-cold processkit context archiving while keeping archived entity metadata queryable th…
    # "context-grooming", # explicitly enable; processkit; Review and prune the project context — archive completed work, summarize stale entries, and propose disabling…
    # "cross-reference-management", # explicitly enable; processkit; Conventions and helpers for lightweight relationships between entities expressed as frontmatter references (n…
    # "data-pipeline", # explicitly enable; data-ai; Data pipeline patterns — ETL/ELT, batch vs streaming, idempotency, orchestration. Use when designing a data p…
    # "data-quality", # explicitly enable; data-ai; Data quality framework — completeness, accuracy, consistency, validation, and contracts. Use when implementin…
    # "data-science", # explicitly enable; data-ai; Data analysis workflow from import through modeling and communication. Use when analyzing a dataset, explorin…
    # "data-storytelling", # explicitly enable; documents; Translates data analysis into clear narratives and recommendations that non-technical audiences can act on. U…
    # "data-visualization", # explicitly enable; documents; Chart selection, color accessibility, annotation, and dashboard design. Use when creating charts, designing d…
    # "database-migration", # explicitly enable; engineering; Schema migration workflows — zero-downtime expand/contract, batched backfills, and rollback strategies. Use w…
    # "database-modeling", # explicitly enable; engineering; Data modeling — ER diagrams, normalization, denormalization trade-offs, relationship patterns, and schema evo…
    # "debugging", # explicitly enable; engineering; Systematic debugging — reproduce, read the error, isolate, hypothesize, fix, verify, document. Use when track…
    # "decision-record", # explicitly enable; processkit; Record decisions with rationale, alternatives, and consequences — the ADR pattern as a primitive. Use when a …
    # "dependency-audit", # explicitly enable; engineering; Audit project dependencies for vulnerabilities and outdated packages. Use when checking the security posture …
    # "dependency-management", # explicitly enable; engineering; Cross-language dependency management — lockfiles, version pinning, automated updates, security, and licenses.…
    # "devils-advocate", # explicitly enable; product; Systematically challenges a plan, decision, or proposal — identifying assumptions, failure modes, and stronge…
    # "discussion-management", # explicitly enable; processkit; Manage Discussion entities — structured, multi-turn conversations that explore questions and produce decision…
    # "distributed-tracing", # explicitly enable; devops; Distributed tracing with OpenTelemetry — spans, context propagation, sampling. Use when instrumenting a distr…
    # "dns-networking", # explicitly enable; devops; DNS records, IP addressing, subnetting, common protocols, and diagnostic tools. Use when configuring DNS reco…
    # "dockerfile-review", # explicitly enable; devops; Dockerfile best practices — layer optimization, caching, security, image size. Use when writing, reviewing, o…
    # "documentation", # explicitly enable; product; Writing effective project documentation — READMEs, API doc comments, and inline comments that explain why. Us…
    # "docx-authoring", # explicitly enable; documents; Creating Word documents programmatically with python-docx — headings, styles, tables, lists, images, and sect…
    # "domain-driven-design", # explicitly enable; engineering; Domain-Driven Design strategic and tactical patterns — bounded contexts, aggregates, value objects, context m…
    # "email-drafter", # explicitly enable; product; Drafts clear, professional emails ready to copy and paste — from a brief description of the situation, audien…
    # "embedding-vectordb", # explicitly enable; data-ai; Vector embeddings and vector DB patterns — model choice, similarity metrics, index tuning. Use when choosing …
    # "error-handling", # explicitly enable; engineering; Error handling across languages — Result types, exceptions, retries, circuit breakers, and structured error r…
    # "estimation-planning", # explicitly enable; product; Software estimation and planning — story points, velocity, scope negotiation, technical debt budgeting, and s…
    # "eval-gate-authoring", # explicitly enable; processkit; Turn observed run outputs into eval-spec Artifacts, paired Gates, and policy bindings. Use when creating or c…
    # "event-driven-architecture", # explicitly enable; engineering; Event-driven systems — pub/sub, event sourcing, CQRS, sagas, broker selection, reliability. Use when designin…
    # "event-log", # explicitly enable; processkit; Append-only event log — the probabilistic record of everything that happened in the project. Use whenever som…
    # "excalidraw", # explicitly enable; design; Generates Excalidraw diagrams programmatically as hand-drawn-style JSON. Use when creating architecture diagr…
    # "fastapi-patterns", # explicitly enable; engineering; FastAPI patterns — dependency injection, Pydantic models, async endpoints, middleware, and testing. Use when …
    # "feature-engineering", # explicitly enable; data-ai; Feature engineering for ML — encoding, imputation, scaling, selection, time-series features. Use when prepari…
    # "flutter-development", # explicitly enable; engineering; Flutter and Dart development — widget architecture, state management, navigation, and cross-platform layout. …
    # "frontend-design", # explicitly enable; design; Frontend architecture and UI design — component hierarchies, accessibility, performance, state management. Us…
    # "gate-management", # explicitly enable; processkit; Manage Gate entities — validation checkpoints that WorkItems, scopes, or policy applications must pass (code …
    # "git-workflow", # explicitly enable; engineering; Git workflow conventions — branch naming, conventional commits, PR descriptions, and merge strategy. Use when…
    # "go-conventions", # explicitly enable; engineering; Go idioms — error wrapping, small interfaces, goroutine lifecycle, table-driven tests. Use when writing or re…
    # "graphql-patterns", # explicitly enable; engineering; GraphQL schema design, resolver patterns, DataLoader, pagination, federation, and schema evolution. Use when …
    # "grpc-protobuf", # explicitly enable; engineering; Protobuf schema design and gRPC service patterns — streaming, error handling, backward compatibility. Use whe…
    # "id-management", # explicitly enable; processkit; Generate unique entity IDs following the project's configured format. The write-side foundation that every en…
    # "incident-response", # explicitly enable; devops; Production incident handling — triage, communicate, mitigate, fix, postmortem. Use when production is broken,…
    # "index-management", # explicitly enable; processkit; SQLite-backed index over all entity files in the project. The read-side foundation for every other MCP server…
    # "infographics", # explicitly enable; documents; Creates data-driven infographics and charts as accessible SVG. Use when visualizing data, choosing a chart ty…
    # "integration-testing", # explicitly enable; engineering; Integration and E2E testing patterns — testcontainers, fixtures, API mocking, snapshots, and CI isolation. Us…
    # "java-patterns", # explicitly enable; engineering; Modern Java 17+ — records, sealed types, pattern matching, streams, Spring Boot. Use when writing or reviewin…
    # "kubernetes-basics", # explicitly enable; devops; Kubernetes cluster management — resources, networking, storage, Helm, troubleshooting. Use when writing Kuber…
    # "latex-authoring", # explicitly enable; documents; LaTeX authoring with LuaLaTeX — document classes, math, TikZ, BibLaTeX. Use when writing or editing LaTeX doc…
    # "legal-review", # explicitly enable; product; Structures a systematic legal and compliance review of contracts, policies, or technical decisions — identify…
    # "linux-administration", # explicitly enable; devops; Linux sysadmin for developers — permissions, processes, systemd, journald, cron, disks. Use when managing Lin…
    # "llm-evaluation", # explicitly enable; data-ai; LLM output evaluation — automated metrics, LLM-as-judge, A/B testing, regression testing. Use when measuring …
    # "load-testing", # explicitly enable; engineering; Load testing methodology — test types, scenario design, capacity planning, CI integration. Use when planning …
    # "logging-strategy", # explicitly enable; devops; Structured logging strategy — levels, correlation IDs, context propagation, PII avoidance. Use when designing…
    # "logo-design", # explicitly enable; design; Creates SVG logos with scalable geometry, color theory, and variant generation. Use when designing logos, ico…
    # "metrics-management", # explicitly enable; devops; Manage metric specifications and observations: quantified measures the project cares about (velocity, error r…
    # "metrics-monitoring", # explicitly enable; devops; Application metrics with RED/USE methods, Prometheus types, dashboards, and SLO alerting. Use when instrument…
    # "microservice-creation", # explicitly enable; engineering; Designs and scaffolds new microservices — covering service boundary identification, inter-service communicati…
    # "migration-management", # explicitly enable; processkit; Manage Migration entities — pending, in-progress, and applied transitions between upstream source versions. U…
    # "ml-pipeline", # explicitly enable; data-ai; ML pipeline design — data versioning, experiment tracking, deployment patterns, drift monitoring. Use when bu…
    # "mobile-app-design", # explicitly enable; design; Mobile app UX — touch targets, navigation, platform conventions, accessibility. Use when designing mobile int…
    # "model-recommender", # explicitly enable; processkit; Recommend the right AI model for a task by scoring candidates across six dimensions (Reasoning, Engineering, …
    # "nosql-patterns", # explicitly enable; engineering; NoSQL patterns for document, key-value, wide-column, and graph stores. Access-pattern-driven design and consi…
    # "note-management", # explicitly enable; processkit; Captures, reviews, and promotes Note entities — the lightweight knowledge capture primitive in processkit. No…
    # "onboarding-guide", # explicitly enable; product; Generates a structured onboarding guide for a new agent or human joining a project — covering codebase orient…
    # "owner-profiling", # explicitly enable; processkit; Build and maintain a structured personal-context portfolio for the project owner — identity, working style, g…
    # "pandas-polars", # explicitly enable; data-ai; DataFrame operations with pandas and polars — groupby, joins, reshaping, performance. Use when manipulating t…
    # "pdf-workflow", # explicitly enable; documents; Generating and manipulating PDF files — from HTML, DOCX, or raw content using reportlab, weasyprint, or pypdf…
    # "performance-profiling", # explicitly enable; engineering; Performance analysis and profiling for CPU, memory, and I/O — flame graphs, benchmarks, regression detection.…
    # "pixijs-gamedev", # explicitly enable; engineering; PixiJS 2D rendering and game development — sprites, animation loops, interaction, and WebGL/Canvas rendering.…
    # "pk-doctor", # explicitly enable; processkit; Aggregator health-check for a processkit-managed repository. Runs a fixed suite of detect-only checks over th…
    # "postmortem-writing", # explicitly enable; devops; Blameless postmortem writing — timeline, root cause analysis, corrective actions. Use when writing an inciden…
    # "pptx-authoring", # explicitly enable; documents; Creating PowerPoint presentations programmatically with python-pptx — slides, layouts, text boxes, charts, ta…
    # "prd-writing", # explicitly enable; product; Write a Product Requirements Document structured with problem statement, goals, user stories, scope, and succ…
    # "process-management", # explicitly enable; processkit; Legacy/migration guidance for v1 Process entities — declarative workflow definitions with roles, gates, and a…
    # "processkit-gateway", # explicitly enable; processkit; Provider-neutral processkit MCP gateway. Use when a harness should see one processkit MCP surface while proce…
    # "prompt-engineering", # explicitly enable; data-ai; Prompt design patterns — few-shot, chain-of-thought, structured output, injection defense. Use when crafting …
    # "python-best-practices", # explicitly enable; engineering; Python conventions and patterns — typing, testing, project layout, tooling. Use when writing or reviewing Pyt…
    # "rag-engineering", # explicitly enable; data-ai; Retrieval-Augmented Generation pipelines — ingestion, chunking, embedding, vector stores, retrieval, evaluati…
    # "refactoring", # explicitly enable; engineering; Systematic refactoring with Fowler's catalog, GoF patterns, and code smell detection. Use when restructuring …
    # "reflex-python", # explicitly enable; engineering; Reflex Python web framework for full-stack apps in pure Python — components, state, routing, and deployment. …
    # "release-audit", # explicitly enable; processkit; Detect-only pre-release validation sweep over the processkit content tree. Walks entity files, SKILL.md defin…
    # "release-semver", # explicitly enable; devops; Semantic versioning releases — version bumps, changelogs, tags, publishing. Use when preparing a new release:…
    # "research-with-confidence", # explicitly enable; product; Investigates a question systematically before answering — verifying claims through sources, distinguishing kn…
    # "retrospective", # explicitly enable; processkit; Generates a post-release blameless retrospective from project signals — synthesizing DORA-like metrics, WorkI…
    # "role-management", # explicitly enable; processkit; Create and maintain Role entities — named sets of responsibilities that actors can fill. Use when defining a …
    # "rust-conventions", # explicitly enable; engineering; Rust conventions — error handling with anyhow/thiserror, module layout, clippy. Use when writing or reviewing…
    # "schedule-management", # explicitly enable; processkit; Legacy/migration guidance for v1 Schedule entities. In v2, define recurring cadences as Binding(type=time-win…
    # "scope-management", # explicitly enable; processkit; Manage Scope entities — bounded containers grouping related work (sprint, milestone, project, quarter). Use w…
    # "secret-management", # explicitly enable; engineering; Handle secrets safely — env vars, .env files, vaults, rotation. Use when dealing with API keys, passwords, to…
    # "secure-coding", # explicitly enable; engineering; Secure coding practices grounded in the OWASP Top 10. Use when reviewing code for security issues, implementi…
    # "security-projections", # explicitly enable; processkit; Project processkit security policy Artifacts into runtime policy files for Agent-IDS and Tetragon-style enfor…
    # "seo-optimization", # explicitly enable; design; SEO — on-page, technical, structured data, Core Web Vitals, and mobile-first indexing. Use when optimizing a …
    # "session-handover", # explicitly enable; processkit; Write an end-of-session handover LogEntry capturing current state, open threads, next action, and git context…
    # "shell-scripting", # explicitly enable; engineering; Bash scripting — strict mode, quoting, arg parsing, traps, shellcheck compliance. Use when writing or reviewi…
    # "skill-builder", # explicitly enable; processkit; Author a new processkit skill end-to-end — use-case interview, frontmatter, body sections, Gotchas, and trigg…
    # "skill-finder", # explicitly enable; processkit; core; Navigation aid for the processkit skill catalog — maps natural-language cues and task types to the right skil…
    # "skill-gate", # explicitly enable; processkit; core; Session-start meta-skill that enforces the 1% rule: before any action on a processkit entity or context/ file…
    # "skill-reviewer", # explicitly enable; processkit; Audit an existing processkit skill against the 5 Skill Killers, the Agent Skills standard, and Anthropic's tr…
    # "software-architecture", # explicitly enable; engineering; Codebase architecture analysis — patterns, ADRs, structural review, C4 diagrams. Use when evaluating a codeba…
    # "software-modularization", # explicitly enable; engineering; Design and evaluate module and package boundaries for maintainability, low coupling, and AI agent context eff…
    # "sprint-retrospective", # explicitly enable; product; Facilitates team or project retrospectives — what worked, what didn't, action items. Use at the end of a spri…
    # "sql-patterns", # explicitly enable; engineering; SQL query patterns, schema design, and optimization — joins, CTEs, window functions, indexing, and anti-patte…
    # "sql-style-guide", # explicitly enable; engineering; SQL style — snake_case, singular tables, named constraints, one-clause-per-line formatting. Use when writing …
    # "standup-context", # explicitly enable; processkit; Writes a structured standup update as a session.standup LogEntry — capturing what was done, what is in progre…
    # "state-machine-management", # explicitly enable; processkit; Legacy/migration guidance for v1 StateMachine entities. In v2, lifecycle rules are implementation contracts o…
    # "status-briefing", # explicitly enable; processkit; Generates a concise status briefing from project context — synthesizing current state, open decisions, key ri…
    # "status-update-writer", # explicitly enable; processkit; Generates clear, structured status updates for stakeholders — from project context, backlog state, or a brief…
    # "system-design", # explicitly enable; engineering; System design methodology — requirements, capacity estimation, component design, trade-offs. Use when designi…
    # "tailwind", # explicitly enable; engineering; Tailwind CSS v4 — utility-first styling, responsive design, dark mode, and component extraction. Use when bui…
    # "task-router", # explicitly enable; processkit; core; Route a task description to the right processkit skill, project-specific process override, and MCP tool in a …
    # "tdd-workflow", # explicitly enable; engineering; Test-driven development workflow — red-green-refactor, naming, doubles, when to apply. Use when writing code …
    # "team-creator", # explicitly enable; processkit; Compose a provider-neutral AI team by tiering accessible models on cost-efficiency, capability, latency, and …
    # "team-manager", # explicitly enable; processkit; Create and maintain TeamMember entities — humans and named AI personas that participate in the project with p…
    # "terraform-basics", # explicitly enable; devops; Infrastructure-as-code with Terraform/OpenTofu — resources, state, modules, plan/apply. Use when writing `.tf…
    # "testing-strategy", # explicitly enable; engineering; Testing approach — unit vs integration vs E2E, coverage goals, organization. Use when deciding what to test, …
    # "threat-modeling", # explicitly enable; engineering; Threat modeling with STRIDE — data flow diagrams, trust boundaries, risk. Use when designing a new system, re…
    # "typescript-patterns", # explicitly enable; engineering; TypeScript patterns — strict mode, discriminated unions, runtime validation, Result types. Use when writing o…
    # "user-research", # explicitly enable; product; Structures user research — planning sessions, synthesizing findings, and translating insights into product de…
    # "webhook-integration", # explicitly enable; engineering; Webhook design and consumption — payload format, HMAC signatures, idempotency, retries, dead-letter queues, s…
    # "workitem-management", # explicitly enable; processkit; Create, transition, query, and link WorkItems — tasks, stories, bugs, epics. Use when the user asks to add, u…
    # "xlsx-modeling", # explicitly enable; documents; Creating Excel spreadsheets programmatically with openpyxl — worksheets, data, formulas, charts, formatting, …
]
disabled = [
    # "actor-profile", # explicitly disable; processkit; Create and maintain Actor entities — humans and AI agents that participate in the project. Use when adding a …
    # "agent-card", # explicitly disable; processkit; Project processkit agent-card Artifacts into canonical public agent-card JSON files. Use when publishing an a…
    # "agent-management", # explicitly disable; processkit; Orchestrate multi-agent workflows with task decomposition, role assignment, handoff protocols, and per-sessio…
    # "aggregate-mcp", # explicitly disable; processkit; Expose the processkit MCP tool surface through a single stdio server. Use for harnesses that eagerly start ev…
    # "ai-fundamentals", # explicitly disable; data-ai; Explain and apply core ML/AI concepts — model types, training pipelines, evaluation metrics, and neural archi…
    # "alerting-oncall", # explicitly disable; devops; Alert design and on-call practices — severity, runbooks, SLO burn-rate alerting, escalation. Use when designi…
    # "api-design", # explicitly disable; engineering; REST API design — resource naming, HTTP methods, status codes, pagination, versioning, OpenAPI. Use when desi…
    # "artifact-management", # explicitly disable; processkit; Create, retrieve, query, and update Artifacts — tangible deliverables produced by the project. Use when stori…
    # "auth-patterns", # explicitly disable; engineering; Authentication and authorization patterns — OAuth2, JWT, sessions, RBAC/ABAC. Use when implementing login flo…
    # "binding-management", # explicitly disable; processkit; Manage Binding entities — scoped, temporal, many-to-many relationships between v2 entity surfaces. Use when a…
    # "board-of-advisors", # explicitly disable; product; Simulates a panel of expert advisors who each evaluate a problem from their distinct perspective — surfacing …
    # "caching-strategies", # explicitly disable; engineering; Caching patterns — cache-aside, write-through, TTL strategies, invalidation, HTTP caching. Use when adding ca…
    # "category-management", # explicitly disable; processkit; Manage Category entities — taxonomies and classification schemes that group other entities by type, area, tie…
    # "changelog", # explicitly disable; engineering; Generates and maintains CHANGELOG.md files following Keep a Changelog conventions — from git history, commit …
    # "ci-cd-setup", # explicitly disable; devops; CI/CD pipeline setup — GitHub Actions, testing, linting, deployment. Use when setting up CI from scratch, add…
    # "code-generation", # explicitly disable; engineering; Code generation patterns — template engines, AST manipulation, scaffolding, build-step generation. Use when b…
    # "code-review", # explicitly disable; engineering; Structured code review with a checklist covering correctness, clarity, tests, security, performance, and styl…
    # "concurrency-patterns", # explicitly disable; engineering; Concurrency and parallelism patterns — async/await, threads, actors, channels, deadlock prevention. Use when …
    # "constraint-management", # explicitly disable; processkit; Manage Constraint entities — rules and limits the project must respect (budget, latency SLO, team size, compl…
    # "container-orchestration", # explicitly disable; devops; Docker Compose patterns for multi-service apps — health checks, networking, volumes, dependencies. Use when w…
    # "context-archiving", # explicitly disable; processkit; Plan and execute hot-to-cold processkit context archiving while keeping archived entity metadata queryable th…
    # "context-grooming", # explicitly disable; processkit; Review and prune the project context — archive completed work, summarize stale entries, and propose disabling…
    # "cross-reference-management", # explicitly disable; processkit; Conventions and helpers for lightweight relationships between entities expressed as frontmatter references (n…
    # "data-pipeline", # explicitly disable; data-ai; Data pipeline patterns — ETL/ELT, batch vs streaming, idempotency, orchestration. Use when designing a data p…
    # "data-quality", # explicitly disable; data-ai; Data quality framework — completeness, accuracy, consistency, validation, and contracts. Use when implementin…
    # "data-science", # explicitly disable; data-ai; Data analysis workflow from import through modeling and communication. Use when analyzing a dataset, explorin…
    # "data-storytelling", # explicitly disable; documents; Translates data analysis into clear narratives and recommendations that non-technical audiences can act on. U…
    # "data-visualization", # explicitly disable; documents; Chart selection, color accessibility, annotation, and dashboard design. Use when creating charts, designing d…
    # "database-migration", # explicitly disable; engineering; Schema migration workflows — zero-downtime expand/contract, batched backfills, and rollback strategies. Use w…
    # "database-modeling", # explicitly disable; engineering; Data modeling — ER diagrams, normalization, denormalization trade-offs, relationship patterns, and schema evo…
    # "debugging", # explicitly disable; engineering; Systematic debugging — reproduce, read the error, isolate, hypothesize, fix, verify, document. Use when track…
    # "decision-record", # explicitly disable; processkit; Record decisions with rationale, alternatives, and consequences — the ADR pattern as a primitive. Use when a …
    # "dependency-audit", # explicitly disable; engineering; Audit project dependencies for vulnerabilities and outdated packages. Use when checking the security posture …
    # "dependency-management", # explicitly disable; engineering; Cross-language dependency management — lockfiles, version pinning, automated updates, security, and licenses.…
    # "devils-advocate", # explicitly disable; product; Systematically challenges a plan, decision, or proposal — identifying assumptions, failure modes, and stronge…
    # "discussion-management", # explicitly disable; processkit; Manage Discussion entities — structured, multi-turn conversations that explore questions and produce decision…
    # "distributed-tracing", # explicitly disable; devops; Distributed tracing with OpenTelemetry — spans, context propagation, sampling. Use when instrumenting a distr…
    # "dns-networking", # explicitly disable; devops; DNS records, IP addressing, subnetting, common protocols, and diagnostic tools. Use when configuring DNS reco…
    # "dockerfile-review", # explicitly disable; devops; Dockerfile best practices — layer optimization, caching, security, image size. Use when writing, reviewing, o…
    # "documentation", # explicitly disable; product; Writing effective project documentation — READMEs, API doc comments, and inline comments that explain why. Us…
    # "docx-authoring", # explicitly disable; documents; Creating Word documents programmatically with python-docx — headings, styles, tables, lists, images, and sect…
    # "domain-driven-design", # explicitly disable; engineering; Domain-Driven Design strategic and tactical patterns — bounded contexts, aggregates, value objects, context m…
    # "email-drafter", # explicitly disable; product; Drafts clear, professional emails ready to copy and paste — from a brief description of the situation, audien…
    # "embedding-vectordb", # explicitly disable; data-ai; Vector embeddings and vector DB patterns — model choice, similarity metrics, index tuning. Use when choosing …
    # "error-handling", # explicitly disable; engineering; Error handling across languages — Result types, exceptions, retries, circuit breakers, and structured error r…
    # "estimation-planning", # explicitly disable; product; Software estimation and planning — story points, velocity, scope negotiation, technical debt budgeting, and s…
    # "eval-gate-authoring", # explicitly disable; processkit; Turn observed run outputs into eval-spec Artifacts, paired Gates, and policy bindings. Use when creating or c…
    # "event-driven-architecture", # explicitly disable; engineering; Event-driven systems — pub/sub, event sourcing, CQRS, sagas, broker selection, reliability. Use when designin…
    # "event-log", # explicitly disable; processkit; Append-only event log — the probabilistic record of everything that happened in the project. Use whenever som…
    # "excalidraw", # explicitly disable; design; Generates Excalidraw diagrams programmatically as hand-drawn-style JSON. Use when creating architecture diagr…
    # "fastapi-patterns", # explicitly disable; engineering; FastAPI patterns — dependency injection, Pydantic models, async endpoints, middleware, and testing. Use when …
    # "feature-engineering", # explicitly disable; data-ai; Feature engineering for ML — encoding, imputation, scaling, selection, time-series features. Use when prepari…
    # "flutter-development", # explicitly disable; engineering; Flutter and Dart development — widget architecture, state management, navigation, and cross-platform layout. …
    # "frontend-design", # explicitly disable; design; Frontend architecture and UI design — component hierarchies, accessibility, performance, state management. Us…
    # "gate-management", # explicitly disable; processkit; Manage Gate entities — validation checkpoints that WorkItems, scopes, or policy applications must pass (code …
    # "git-workflow", # explicitly disable; engineering; Git workflow conventions — branch naming, conventional commits, PR descriptions, and merge strategy. Use when…
    # "go-conventions", # explicitly disable; engineering; Go idioms — error wrapping, small interfaces, goroutine lifecycle, table-driven tests. Use when writing or re…
    # "graphql-patterns", # explicitly disable; engineering; GraphQL schema design, resolver patterns, DataLoader, pagination, federation, and schema evolution. Use when …
    # "grpc-protobuf", # explicitly disable; engineering; Protobuf schema design and gRPC service patterns — streaming, error handling, backward compatibility. Use whe…
    # "id-management", # explicitly disable; processkit; Generate unique entity IDs following the project's configured format. The write-side foundation that every en…
    # "incident-response", # explicitly disable; devops; Production incident handling — triage, communicate, mitigate, fix, postmortem. Use when production is broken,…
    # "index-management", # explicitly disable; processkit; SQLite-backed index over all entity files in the project. The read-side foundation for every other MCP server…
    # "infographics", # explicitly disable; documents; Creates data-driven infographics and charts as accessible SVG. Use when visualizing data, choosing a chart ty…
    # "integration-testing", # explicitly disable; engineering; Integration and E2E testing patterns — testcontainers, fixtures, API mocking, snapshots, and CI isolation. Us…
    # "java-patterns", # explicitly disable; engineering; Modern Java 17+ — records, sealed types, pattern matching, streams, Spring Boot. Use when writing or reviewin…
    # "kubernetes-basics", # explicitly disable; devops; Kubernetes cluster management — resources, networking, storage, Helm, troubleshooting. Use when writing Kuber…
    # "latex-authoring", # explicitly disable; documents; LaTeX authoring with LuaLaTeX — document classes, math, TikZ, BibLaTeX. Use when writing or editing LaTeX doc…
    # "legal-review", # explicitly disable; product; Structures a systematic legal and compliance review of contracts, policies, or technical decisions — identify…
    # "linux-administration", # explicitly disable; devops; Linux sysadmin for developers — permissions, processes, systemd, journald, cron, disks. Use when managing Lin…
    # "llm-evaluation", # explicitly disable; data-ai; LLM output evaluation — automated metrics, LLM-as-judge, A/B testing, regression testing. Use when measuring …
    # "load-testing", # explicitly disable; engineering; Load testing methodology — test types, scenario design, capacity planning, CI integration. Use when planning …
    # "logging-strategy", # explicitly disable; devops; Structured logging strategy — levels, correlation IDs, context propagation, PII avoidance. Use when designing…
    # "logo-design", # explicitly disable; design; Creates SVG logos with scalable geometry, color theory, and variant generation. Use when designing logos, ico…
    # "metrics-management", # explicitly disable; devops; Manage metric specifications and observations: quantified measures the project cares about (velocity, error r…
    # "metrics-monitoring", # explicitly disable; devops; Application metrics with RED/USE methods, Prometheus types, dashboards, and SLO alerting. Use when instrument…
    # "microservice-creation", # explicitly disable; engineering; Designs and scaffolds new microservices — covering service boundary identification, inter-service communicati…
    # "migration-management", # explicitly disable; processkit; Manage Migration entities — pending, in-progress, and applied transitions between upstream source versions. U…
    # "ml-pipeline", # explicitly disable; data-ai; ML pipeline design — data versioning, experiment tracking, deployment patterns, drift monitoring. Use when bu…
    # "mobile-app-design", # explicitly disable; design; Mobile app UX — touch targets, navigation, platform conventions, accessibility. Use when designing mobile int…
    # "model-recommender", # explicitly disable; processkit; Recommend the right AI model for a task by scoring candidates across six dimensions (Reasoning, Engineering, …
    # "nosql-patterns", # explicitly disable; engineering; NoSQL patterns for document, key-value, wide-column, and graph stores. Access-pattern-driven design and consi…
    # "note-management", # explicitly disable; processkit; Captures, reviews, and promotes Note entities — the lightweight knowledge capture primitive in processkit. No…
    # "onboarding-guide", # explicitly disable; product; Generates a structured onboarding guide for a new agent or human joining a project — covering codebase orient…
    # "owner-profiling", # explicitly disable; processkit; Build and maintain a structured personal-context portfolio for the project owner — identity, working style, g…
    # "pandas-polars", # explicitly disable; data-ai; DataFrame operations with pandas and polars — groupby, joins, reshaping, performance. Use when manipulating t…
    # "pdf-workflow", # explicitly disable; documents; Generating and manipulating PDF files — from HTML, DOCX, or raw content using reportlab, weasyprint, or pypdf…
    # "performance-profiling", # explicitly disable; engineering; Performance analysis and profiling for CPU, memory, and I/O — flame graphs, benchmarks, regression detection.…
    # "pixijs-gamedev", # explicitly disable; engineering; PixiJS 2D rendering and game development — sprites, animation loops, interaction, and WebGL/Canvas rendering.…
    # "pk-doctor", # explicitly disable; processkit; Aggregator health-check for a processkit-managed repository. Runs a fixed suite of detect-only checks over th…
    # "postmortem-writing", # explicitly disable; devops; Blameless postmortem writing — timeline, root cause analysis, corrective actions. Use when writing an inciden…
    # "pptx-authoring", # explicitly disable; documents; Creating PowerPoint presentations programmatically with python-pptx — slides, layouts, text boxes, charts, ta…
    # "prd-writing", # explicitly disable; product; Write a Product Requirements Document structured with problem statement, goals, user stories, scope, and succ…
    # "process-management", # explicitly disable; processkit; Legacy/migration guidance for v1 Process entities — declarative workflow definitions with roles, gates, and a…
    # "processkit-gateway", # explicitly disable; processkit; Provider-neutral processkit MCP gateway. Use when a harness should see one processkit MCP surface while proce…
    # "prompt-engineering", # explicitly disable; data-ai; Prompt design patterns — few-shot, chain-of-thought, structured output, injection defense. Use when crafting …
    # "python-best-practices", # explicitly disable; engineering; Python conventions and patterns — typing, testing, project layout, tooling. Use when writing or reviewing Pyt…
    # "rag-engineering", # explicitly disable; data-ai; Retrieval-Augmented Generation pipelines — ingestion, chunking, embedding, vector stores, retrieval, evaluati…
    # "refactoring", # explicitly disable; engineering; Systematic refactoring with Fowler's catalog, GoF patterns, and code smell detection. Use when restructuring …
    # "reflex-python", # explicitly disable; engineering; Reflex Python web framework for full-stack apps in pure Python — components, state, routing, and deployment. …
    # "release-audit", # explicitly disable; processkit; Detect-only pre-release validation sweep over the processkit content tree. Walks entity files, SKILL.md defin…
    # "release-semver", # explicitly disable; devops; Semantic versioning releases — version bumps, changelogs, tags, publishing. Use when preparing a new release:…
    # "research-with-confidence", # explicitly disable; product; Investigates a question systematically before answering — verifying claims through sources, distinguishing kn…
    # "retrospective", # explicitly disable; processkit; Generates a post-release blameless retrospective from project signals — synthesizing DORA-like metrics, WorkI…
    # "role-management", # explicitly disable; processkit; Create and maintain Role entities — named sets of responsibilities that actors can fill. Use when defining a …
    # "rust-conventions", # explicitly disable; engineering; Rust conventions — error handling with anyhow/thiserror, module layout, clippy. Use when writing or reviewing…
    # "schedule-management", # explicitly disable; processkit; Legacy/migration guidance for v1 Schedule entities. In v2, define recurring cadences as Binding(type=time-win…
    # "scope-management", # explicitly disable; processkit; Manage Scope entities — bounded containers grouping related work (sprint, milestone, project, quarter). Use w…
    # "secret-management", # explicitly disable; engineering; Handle secrets safely — env vars, .env files, vaults, rotation. Use when dealing with API keys, passwords, to…
    # "secure-coding", # explicitly disable; engineering; Secure coding practices grounded in the OWASP Top 10. Use when reviewing code for security issues, implementi…
    # "security-projections", # explicitly disable; processkit; Project processkit security policy Artifacts into runtime policy files for Agent-IDS and Tetragon-style enfor…
    # "seo-optimization", # explicitly disable; design; SEO — on-page, technical, structured data, Core Web Vitals, and mobile-first indexing. Use when optimizing a …
    # "session-handover", # explicitly disable; processkit; Write an end-of-session handover LogEntry capturing current state, open threads, next action, and git context…
    # "shell-scripting", # explicitly disable; engineering; Bash scripting — strict mode, quoting, arg parsing, traps, shellcheck compliance. Use when writing or reviewi…
    # "skill-builder", # explicitly disable; processkit; Author a new processkit skill end-to-end — use-case interview, frontmatter, body sections, Gotchas, and trigg…
    # "skill-finder", # explicitly disable; processkit; core; Navigation aid for the processkit skill catalog — maps natural-language cues and task types to the right skil…
    # "skill-gate", # explicitly disable; processkit; core; Session-start meta-skill that enforces the 1% rule: before any action on a processkit entity or context/ file…
    # "skill-reviewer", # explicitly disable; processkit; Audit an existing processkit skill against the 5 Skill Killers, the Agent Skills standard, and Anthropic's tr…
    # "software-architecture", # explicitly disable; engineering; Codebase architecture analysis — patterns, ADRs, structural review, C4 diagrams. Use when evaluating a codeba…
    # "software-modularization", # explicitly disable; engineering; Design and evaluate module and package boundaries for maintainability, low coupling, and AI agent context eff…
    # "sprint-retrospective", # explicitly disable; product; Facilitates team or project retrospectives — what worked, what didn't, action items. Use at the end of a spri…
    # "sql-patterns", # explicitly disable; engineering; SQL query patterns, schema design, and optimization — joins, CTEs, window functions, indexing, and anti-patte…
    # "sql-style-guide", # explicitly disable; engineering; SQL style — snake_case, singular tables, named constraints, one-clause-per-line formatting. Use when writing …
    # "standup-context", # explicitly disable; processkit; Writes a structured standup update as a session.standup LogEntry — capturing what was done, what is in progre…
    # "state-machine-management", # explicitly disable; processkit; Legacy/migration guidance for v1 StateMachine entities. In v2, lifecycle rules are implementation contracts o…
    # "status-briefing", # explicitly disable; processkit; Generates a concise status briefing from project context — synthesizing current state, open decisions, key ri…
    # "status-update-writer", # explicitly disable; processkit; Generates clear, structured status updates for stakeholders — from project context, backlog state, or a brief…
    # "system-design", # explicitly disable; engineering; System design methodology — requirements, capacity estimation, component design, trade-offs. Use when designi…
    # "tailwind", # explicitly disable; engineering; Tailwind CSS v4 — utility-first styling, responsive design, dark mode, and component extraction. Use when bui…
    # "task-router", # explicitly disable; processkit; core; Route a task description to the right processkit skill, project-specific process override, and MCP tool in a …
    # "tdd-workflow", # explicitly disable; engineering; Test-driven development workflow — red-green-refactor, naming, doubles, when to apply. Use when writing code …
    # "team-creator", # explicitly disable; processkit; Compose a provider-neutral AI team by tiering accessible models on cost-efficiency, capability, latency, and …
    # "team-manager", # explicitly disable; processkit; Create and maintain TeamMember entities — humans and named AI personas that participate in the project with p…
    # "terraform-basics", # explicitly disable; devops; Infrastructure-as-code with Terraform/OpenTofu — resources, state, modules, plan/apply. Use when writing `.tf…
    # "testing-strategy", # explicitly disable; engineering; Testing approach — unit vs integration vs E2E, coverage goals, organization. Use when deciding what to test, …
    # "threat-modeling", # explicitly disable; engineering; Threat modeling with STRIDE — data flow diagrams, trust boundaries, risk. Use when designing a new system, re…
    # "typescript-patterns", # explicitly disable; engineering; TypeScript patterns — strict mode, discriminated unions, runtime validation, Result types. Use when writing o…
    # "user-research", # explicitly disable; product; Structures user research — planning sessions, synthesizing findings, and translating insights into product de…
    # "webhook-integration", # explicitly disable; engineering; Webhook design and consumption — payload format, HMAC signatures, idempotency, retries, dead-letter queues, s…
    # "workitem-management", # explicitly disable; processkit; Create, transition, query, and link WorkItems — tasks, stories, bugs, epics. Use when the user asks to add, u…
    # "xlsx-modeling", # explicitly disable; documents; Creating Excel spreadsheets programmatically with openpyxl — worksheets, data, formulas, charts, formatting, …
]

# =============================================================================
# [addons] — language runtimes and tool bundles
# =============================================================================
# Each addon installs a tool set into the container at build time.
# Selected addons land here pre-populated with default-enabled tools at
# their default versions; edit the version strings to switch.
#
# Version strings:
#   "1.2.3"  — pin to a specific version
#   "latest" — always install the newest version (skips pinning)
#   ""       — use the addon's built-in default version
#
# Run `aibox get addon` to see all available addons.
# Run `aibox describe addon <name>` to see every supported tool/version per addon.
#
# To add an addon after init, edit this file and re-run `aibox apply`,
# or use `aibox set addon <name>` (which also pulls in transitive `requires`).
#
# Addon catalog — uncomment/comment one block header to enable or remove an addon.
# Inside an enabled addon, omitted default-enabled tools stay enabled. Uncomment
# a tool line to pin a version, enable an off-by-default tool, or disable a default-on tool.
#

# ---- AI Providers ------------------------------------------------------------

# AI Providers/ai-aider — Aider – AI pair programming via CLI
# [addons.ai-aider.tools]
# aider = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }

# AI Providers/ai-claude — Claude Code CLI (Anthropic)
[addons.ai-claude.tools]
claude = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }

# AI Providers/ai-codex — OpenAI Codex CLI; requires node
[addons.ai-codex.tools]
codex = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }

# AI Providers/ai-continue — Continue CLI – provider-agnostic coding agent
# [addons.ai-continue.tools]
# cn = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }

# AI Providers/ai-copilot — GitHub Copilot CLI
# [addons.ai-copilot.tools]
# copilot = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }

# AI Providers/ai-gemini — Google Gemini CLI
# [addons.ai-gemini.tools]
# gemini = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }

# AI Providers/ai-hermes — Hermes autonomous AI agent (Nous Research, multi-provider)
# [addons.ai-hermes.tools]
# hermes = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }

# AI Providers/ai-mistral — Mistral AI Python SDK (programmatic use)
# [addons.ai-mistral.tools]
# mistral = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }

# AI Providers/ai-opencode — OpenCode AI coding agent (open-source, multi-provider)
# [addons.ai-opencode.tools]
# opencode = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }

# ---- Documentation ------------------------------------------------------------

# Documentation/docs-docusaurus — Docusaurus static docs site (React/MDX); requires node
[addons.docs-docusaurus.tools]
docusaurus = { version = "3.8" } # default on; options: {}, { enabled = true|false }, { version = "3.7.0" (default) or "latest" }

# Documentation/docs-hugo — Hugo static site generator (Go)
# [addons.docs-hugo.tools]
# hugo = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }

# Documentation/docs-mdbook — mdBook – Rust-powered book builder
# [addons.docs-mdbook.tools]
# mdbook = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }

# Documentation/docs-mkdocs — MkDocs – Python Markdown docs site; requires python
# [addons.docs-mkdocs.tools]
# mkdocs = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }

# Documentation/docs-starlight — Starlight – Astro-based docs site; requires node
# [addons.docs-starlight.tools]
# starlight = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }

# Documentation/docs-zensical — Zensical – Python docs site; requires python
# [addons.docs-zensical.tools]
# zensical = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }

# ---- Languages ------------------------------------------------------------

# Languages/go — Go programming language toolchain
# [addons.go.tools]
# go = {} # default on; options: {}, { enabled = true|false }, { version = "1.25" | "1.26" (default) or "latest" }

# Languages/latex — TeX Live LaTeX typesetting system
# [addons.latex.tools]
# texlive-core = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# texlive-recommended = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# texlive-fonts = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# texlive-biber = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# texlive-code = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# texlive-diagrams = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# texlive-math = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# texlive-music = { enabled = true } # default off; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# texlive-chemistry = { enabled = true } # default off; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# texlive-linguistics = { enabled = true } # default off; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# svg-inkscape = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }

# Languages/node — Node.js runtime with pnpm, yarn, or bun
[addons.node.tools]
node = { version = "22" } # default on; options: {}, { enabled = true|false }, { version = "20" | "22" (default) or "latest" }
pnpm = { version = "10" } # default on; options: {}, { enabled = true|false }, { version = "9" | "10" (default) or "latest" }
# yarn = { enabled = true } # default off; options: {}, { enabled = true|false }, { version = "4" (default) or "latest" }
# bun = { enabled = true } # default off; options: {}, { enabled = true|false }, { version = "1.2" (default) or "latest" }

# Languages/python — Python runtime with uv, poetry, or pdm
[addons.python.tools]
python = { version = "3.13" } # default on; options: {}, { enabled = true|false }, { version = "3.12" | "3.13" (default) | "3.14" or "latest" }
uv = { version = "0.7" } # default on; options: {}, { enabled = true|false }, { version = "0.6" | "0.7" (default) or "latest" }
# pip = { enabled = true } # default off; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# poetry = { enabled = true } # default off; options: {}, { enabled = true|false }, { version = "1.8" | "2.0" (default) or "latest" }
# pdm = { enabled = true } # default off; options: {}, { enabled = true|false }, { version = "2.22" (default) or "latest" }

# Languages/rust — Rust toolchain via rustup
# [addons.rust.tools]
# rustc = {} # default on; options: {}, { enabled = true|false }, { version = "1.90" | "1.91" | "1.92" | "1.93" | "1.94" (default) or "latest" }
# clippy = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# rustfmt = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# cargo-audit = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# x86_64-cross = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }

# Languages/typst — Typst – modern typesetting system
# [addons.typst.tools]
# typst = {} # default on; options: {}, { enabled = true|false }, { version = "0.13.1" | "0.14.2" (default) or "latest" }

# ---- Tools ------------------------------------------------------------

# Tools/audio-voice — PulseAudio and Sox tools for interactive voice workflows
[addons.audio-voice.tools]
# sox = { enabled = false } # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# sox-pulse = { enabled = false } # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# pulseaudio-utils = { enabled = false } # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# alsa-pulse = { enabled = false } # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }

# Tools/cloud-aws — AWS CLI v2
# [addons.cloud-aws.tools]
# aws-cli = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }

# Tools/cloud-azure — Azure CLI
# [addons.cloud-azure.tools]
# azure-cli = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }

# Tools/cloud-gcp — Google Cloud SDK (gcloud)
# [addons.cloud-gcp.tools]
# gcloud-cli = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }

# Tools/data-preview — SQLite and tabular data preview tools for Yazi
# [addons.data-preview.tools]
# sqlite3 = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# csvkit = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }

# Tools/git-ui — GitHub CLI and lazygit interactive Git UI
[addons.git-ui.tools]
gh = { enabled = true } # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
lazygit = { enabled = false } # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }

# Tools/infrastructure — OpenTofu, Ansible, Packer
# [addons.infrastructure.tools]
# opentofu = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# ansible = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# packer = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }

# Tools/kubernetes — kubectl, Helm, Kustomize, k9s
# [addons.kubernetes.tools]
# kubectl = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# helm = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# kustomize = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# k9s = { enabled = true } # default off; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }

# Tools/preview-archive — Yazi image/PDF/SVG preview helpers plus archive preview/extraction tools
# [addons.preview-archive.tools]
# chafa = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# librsvg = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# poppler = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# timg = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# mupdf = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# entr = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# p7zip = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# ouch = {} # default on; options: {}, { enabled = true|false }, { version = "0.7.1" (default) or "latest" }
# resvg = {} # default on; options: {}, { enabled = true|false }, { version = "0.47.0" (default) or "latest" }

# Tools/preview-enhanced — Rich Markdown rendering, ffmpeg, ImageMagick, and Ghostscript; requires preview-archive
# [addons.preview-enhanced.tools]
# ffmpeg = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# imagemagick = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# ghostscript = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }
# rich = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }

# Tools/yazi-omp — oh-my-posh for the omp.yazi header prompt
# [addons.yazi-omp.tools]
# oh-my-posh = {} # default on; options: {}, { enabled = true|false }, { version = "x.y.z" or "latest" }

# =============================================================================
# [ai] — AI agent harnesses and model providers
# =============================================================================
# Harnesses: CLI tools installed in the container.
# Harness (CLI tool)          Config value   Provider (API key)
# Claude Code                 claude         Anthropic
# OpenAI Codex                codex          OpenAI
# Gemini CLI                  gemini         Google
# Aider                       aider          any (multi-provider)
# Continue                    continue       any (multi-provider)
# Cursor                      cursor         any (host-side IDE)
# GitHub Copilot              copilot        (uses GITHUB_TOKEN)
# OpenCode                    opencode       any (multi-provider)
# Hermes                      hermes         any (multi-provider)
#
# Model providers (optional): declare which API keys are available.
# Provider     Config value   Env var
# Anthropic    anthropic      ANTHROPIC_API_KEY
# OpenAI       openai         OPENAI_API_KEY
# Google       google         GEMINI_API_KEY
# Mistral      mistral        MISTRAL_API_KEY
[ai]
harnesses = [
#     "claude", # Claude Code (claude)
    "codex", # OpenAI Codex (codex)
#     "gemini", # Gemini CLI (gemini)
#     "aider", # Aider (aider)
#     "continue", # Continue (continue)
#     "cursor", # Cursor (cursor)
#     "copilot", # GitHub Copilot (copilot)
#     "opencode", # OpenCode (opencode)
#     "hermes", # Hermes (hermes)
]

model_providers = [
#     "anthropic", # env: ANTHROPIC_API_KEY
#     "openai", # env: OPENAI_API_KEY
#     "google", # env: GEMINI_API_KEY
#     "mistral", # env: MISTRAL_API_KEY
]

[ai.agents]
canonical     = "AGENTS.md"
provider_mode = "pointer"  # options: pointer, full

# =============================================================================
# [processkit] — content layer source (skills, primitives, processes)
# =============================================================================
# processkit ships the skills and primitives that aibox installs into the
# project. The default upstream is the canonical projectious-work/processkit
# repo. Companies can fork processkit and have their projects consume the fork
# by changing `source` to point at their fork.
#
# `version` is the git tag of the processkit source to consume. Special values:
#   "unset"  — no version pinned yet; processkit content is not installed.
#   "latest" — resolve to the newest available tag at every `aibox apply`.
[processkit]
source   = "https://github.com/projectious-work/processkit.git"
version  = "latest"
src_path = "src"
# branch = "main"   # optional — for tracking a moving branch (discouraged)
#
# Optional release-asset URL template for non-GitHub hosts (Gitea, GitLab,
# self-hosted). When unset, the fetcher uses the GitHub-style default:
#   {source}/releases/download/{version}/{name}-{version}.tar.gz
# Placeholders: {source} (.git stripped), {version}, {org}, {name}.
# release_asset_url_template = "https://gitea.example.com/{org}/{name}/releases/download/{version}/payload.tar.gz"

[processkit.context]
schema_version = "1.0.0"      # Context schema version — updated automatically by `aibox apply`
# packages = ["product"]  # deprecated; full product skill set is the default

# =============================================================================
# [customization] — color theme, shell prompt, and zellij layout
# =============================================================================
# Theme is applied consistently across Zellij, Vim, Yazi, lazygit, and bat.
# Options: gruvbox-dark | catppuccin-mocha | catppuccin-latte | dracula | tokyo-night | nord | projectious
[customization]
theme  = "dracula"
# Global mode overlay. `auto` preserves the selected concrete theme.
# Options: auto | light | dark
mode   = "auto"
# Starship prompt preset.
# Options: default | plain | minimal | nerd-font | pastel | bracketed | arrow
prompt = "arrow"
# Default zellij layout. Options: dev | focus | cowork | cowork-swap | browse | ai
layout = "ai"

# Zellij status presentation. Options: native | shell | hidden
[customization.zellij_status]
mode = "shell"

# =============================================================================
# [container.audio] — PulseAudio bridging for voice features
# =============================================================================
# Requires host-side setup: run `aibox apply audio` on the host first.
[container.audio]
enabled = true
pulse_server = "tcp:host.docker.internal:4714"  # PulseAudio TCP endpoint (default port: 4714)

# =============================================================================
# [ai.mcp] — MCP gateway, permissions, and extra servers
# =============================================================================
# Auto-allow / deny MCP tools by glob pattern. processkit's own MCP tools are
# pre-approved separately via the skill-gate preauth spec — these patterns are
# for user-added MCP servers. See:
# https://projectious-work.github.io/aibox/docs/reference/configuration#permission-configuration-mcppermissions
# [ai.mcp.permissions]
# default_mode   = "ask"
# allow_patterns = []
# deny_patterns  = []

# [ai.mcp.gateway] — processkit MCP topology. Options for mode: auto | granular | stdio | daemon-proxy
[ai.mcp.gateway]
mode = "auto"          # auto uses daemon-proxy when processkit-gateway is installed
lazy_catalog = false    # Use processkit's lazy catalog where supported
host = "127.0.0.1"     # daemon-proxy is always localhost-only
port = 8765
path = "/mcp"
